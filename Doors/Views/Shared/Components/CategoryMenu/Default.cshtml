<li class="dropdown">
    <a asp-controller="Door"
       asp-action="Index"
       class="dropdown-toggle" data-toggle="dropdown">Двери<b class="caret"></b></a>
    <ul class="dropdown-menu">
        @foreach (var category in Model)
        {
            <li>
                <a asp-controller="Door" asp-action="List"
                   asp-route-category="@category.CategoryName">@category.CategoryName</a>
            </li>
        }
        <li class="divider"></li>
        <li>
            <a asp-controller="Door" asp-action="List" asp-route-category="">Показать все</a>
        </li>
    </ul>
</li>